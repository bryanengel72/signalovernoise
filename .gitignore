# Dependencies
node_modules/

# Build output
dist/
build/

# Environment variables
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Scroll-film / hero-loop working files (large intermediates, ffmpeg symlink)
experiments/
# Added by `vercel link`. .env* is deliberately broad, but it also matches the
# committed env template — which only stays tracked because git ignores rules
# for files already in the index. Negated so a re-add cannot silently drop it.
.vercel
.env*
!.env.example
